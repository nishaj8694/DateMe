import ReactDOM from 'react-dom/client';
import './newPassword.css';
import Pas from './newPas/Newpassword'

const root = ReactDOM.createRoot(document.getElementById('pwc'));
root.render(
  <React.StrictMode>
    <Pas />
    {/* <Rote /> */}
  </React.StrictMode>
);
